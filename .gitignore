.DS_Store

# Obsidian — keep shared vault config, ignore per-user/workspace state
.obsidian/workspace
.obsidian/workspace.json
.obsidian/workspace-mobile.json
.obsidian/cache
.trash/
.playright-mcp/

# Keep shared .claude/settings.json; ignore personal/local overrides
.claude/settings.local.json
.copilot/

# Python
__pycache__/
*.pyc
.venv/

# --- whisperx transcriber ---
# Append these lines to your repo's existing .gitignore.

# Hugging Face token used for local runs (never commit secrets).
huggingface.token

# Python virtualenv created by transcribe.sh for local runs.
whisper-env/
